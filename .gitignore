node_modules/
dist/
*.tgz
*.tsbuildinfo
.DS_Store
.playwright-mcp/
test-results/
playwright-report/
playwright/.cache/
.tmp/
*.local

# Local secrets (relay debug TOTP, tunnel URLs, etc.)
.env
.env.*
!.env.example
.ait_relay
.ait_urls

# Claude Code
.claude/settings.local.json
.claude/*.lock
.claude.lock
.claude/worktrees/
.mcp.json

# aitcc dog-food per-run artifacts (not fixtures)
aitcc/submit-*.json

# Granite / @apps-in-toss/cli build artifacts
.granite/
*.ait
.codegraph/

# .ait.test 4-cell 오류-shape 캡처 산출물 (per-run, fixture 아님)
.ait-capture/
