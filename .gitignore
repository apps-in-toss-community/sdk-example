node_modules/
dist/
*.tgz
*.tsbuildinfo
.DS_Store
.playwright-mcp/
test-results/
playwright-report/
playwright/.cache/
.tmp/
*.local

# Local secrets (relay debug TOTP, tunnel URLs, etc.)
.env
.env.*
!.env.example
.ait_relay
.ait_urls

# Claude Code
.claude/settings.local.json
.claude/*.lock
.claude.lock
.claude/worktrees/
.mcp.json

# aitcc dog-food per-run artifacts (not fixtures)
aitcc/submit-*.json

# Granite / @apps-in-toss/cli build artifacts
.granite/
*.ait
.codegraph/

# .ait.test 4-cell 오류-shape 캡처 산출물 (per-run, fixture 아님)
.ait-capture/

# env2(ios-sim) engine replay corpus — AIT_CAPTURE_DIR override 출력 (per-run, fixture 아님)
.ait-capture-env2/

# 비교 대상이 아닌 기질(jsdom) 캡처 — diff corpus 밖으로 격리 (per-run, fixture 아님)
.ait-capture-substrate/

# env3 test run report artifacts (per-run, not fixtures — .ait-run/.ait-capture/ 포함)
.ait-run/
.ait-engine-sim/
