# TODO

조직 전체의 TODO는 umbrella에서 단일 source of truth로 관리한다.
이 repo의 작업 항목은 [`../TODO.md`](../TODO.md)의 `sdk-example` 섹션 참고.

sdk-example은 모든 repo가 수렴하는 downstream consumer / dog-fooding 타겟이라
cross-repo 통합 항목 (`docs` cross-link, `oidc-bridge` auth demo, `console-cli`
실배포, `polyfill` 통합 등) 상당수가 여기로 떨어진다. 이건 umbrella TODO 상단의
"Cross-repo / 통합" 섹션에서 관리한다.
